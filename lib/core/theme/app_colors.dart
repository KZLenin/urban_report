import 'package:flutter/material.dart';

class AppColors {
  // 🟢 Color principal (comunidad / solución)
  static const Color primary = Color(0xFF2E7D32); // verde institucional

  // 🟠 Color secundario (reportes / alertas)
  static const Color secondary = Color(0xFFF57C00); // naranja urbano

  // 🟠 Variante clara para botones secundarios
  static const Color secondaryLight = Color(0xFFFFA726);

  // 🏙️ Fondo general (claro, urbano)
  static const Color background = Color(0xFFF7F9FC);

  // 📝 Textos
  static const Color title = Color(0xFF1B2E1F);      // verde muy oscuro
  static const Color subtitle = Color(0xFF5F6F61);   // gris verdoso
  static const Color body = Color(0xFF37474F);

  // 🔘 Botones
  static const Color buttonText = Colors.white;

  // 📐 Bordes y separadores
  static const Color border = Color(0xFFDDE5DD);

  // 🚦 Estados (perfectos para reportes)
  static const Color success = Color(0xFF388E3C);
  static const Color warning = Color(0xFFF9A825);
  static const Color error = Color(0xFFD32F2F);
}


